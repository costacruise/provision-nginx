dependencies:
  pre:
    - sudo pip install awscli boto fabric fabric-aws-tools

test:
  override:
    - echo "no tests" 

deployment:
  production:
    branch: 99590928-use-correct-bucket
    commands:
      - aws s3 sync config s3://web-costa-de-routing/ --delete 
